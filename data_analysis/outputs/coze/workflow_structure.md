# 工作流结构

1. 开始节点：接收 `user_input`
2. 意图识别（LLM 节点）：返回 `query` 或 `analyze`
3. 条件分支：
   - `query` → LLM搜索知识库并整合回答 → 结束
   - `analyze` → 数据解析（LLM）→ 代码计算（Python）→ 报告生成（LLM）→ 结束
4. 合并输出节点（LLM）：统一格式回复

各节点之间通过输入变量传递结构化数据，避免提示词拼接。